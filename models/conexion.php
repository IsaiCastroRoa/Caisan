<?php
$server="localhost";
$user="root";
$db="writer";
$conexion= mysqli_connect($server,$user,"",$db);
$conexion->set_charset("utf-8");

/* if(!isset($conexion)){
    echo "no";
}else{
    echo "si";
}
 */
?>